---
title: "API authentication"
sidebarTitle: "Authentication"
description: "Authenticate RemitFlex API requests with API keys, understand scopes, and handle auth errors."
openapi: "GET /rates"
---

Business endpoints accept an API key as a Bearer token. Key management requires a dashboard JWT and is not available via API key.

## Authorization header

```
Authorization: Bearer rmf_live_xxxxxxxxSSSSSSSSSSSSSSSSSSSSSS
```

<CodeGroup>

```bash curl
curl -X GET https://api.remitflex.io/v1/rates \
  -H "Authorization: Bearer rmf_test_xxxx"
```

```javascript JavaScript
const res = await fetch("https://api.remitflex.io/v1/rates", {
  headers: { Authorization: `Bearer ${process.env.REMITFLEX_API_KEY}` },
});
```

```python Python
import os, requests
r = requests.get(
    "https://api.remitflex.io/v1/rates",
    headers={"Authorization": f"Bearer {os.environ['REMITFLEX_API_KEY']}"},
)
```

</CodeGroup>

## Key prefixes

| Prefix | Environment |
|--------|-------------|
| `rmf_test_` | Test / sandbox |
| `rmf_live_` | Production |

<Warning>
  Live keys move real funds on Solana and Relay rails. Use `rmf_test_` keys during development.
</Warning>

## Obtaining keys

1. Log in at [dashboard.kaprex.io](https://dashboard.kaprex.io).
2. Go to **Settings → API Keys**.
3. Create a key with the scopes your integration needs.
4. Copy the raw key immediately — it cannot be retrieved later.

## Scopes

API keys use fine-grained scopes. Each endpoint in this reference requires specific scopes when called with an API key.

| Scope | Access |
|-------|--------|
| `fx:read` | Exchange rates |
| `transfers:read` | Read swaps, transactions, customers |
| `transfers:write` | Create/cancel swaps, create customers |
| `collections:read` | Read payment routes and deposits |
| `collections:write` | Create and manage payment routes |

A `403` response with `Missing required scopes: ...` means the key is valid but lacks permission. Create a new key with the required scopes.

## Auth errors

<Tabs>
  <Tab title="401 — Invalid key">
    ```json
    {
      "status": "error",
      "message": "Invalid API key"
    }
    ```
  </Tab>
  <Tab title="403 — Missing scope">
    ```json
    {
      "status": "error",
      "message": "Missing required scopes: transfers:write"
    }
    ```
  </Tab>
  <Tab title="400 — No idempotency key">
    ```json
    {
      "status": "error",
      "message": "Idempotency-Key header is required for mutating API requests"
    }
    ```
  </Tab>
</Tabs>

## Dashboard JWT

The dashboard uses JWT access tokens (`Authorization: Bearer eyJ...`) with full access to business routes. JWT is required for `/v1/auth/*` and `/v1/api-keys/*`.

See the [Authentication guide](/authentication) for the full key-creation workflow.
